<manifest package="org.sugarloader.demo"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name="org.sugarloader.demo.Application"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".OneLoaderActivity"
            android:label="@string/app_name"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".TwoLoadersActivity"
            android:label="@string/title_2_loaders_1_activity"
            android:launchMode="singleTop"/>
        <activity android:name=".TwoFragmentsActivity" android:label="@string/title_2_fragments_2_loader_each"/>
    </application>
</manifest>
